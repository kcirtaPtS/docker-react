language: generic
sudo: required

services:
    -docker

before_install:
    - docker build -t kcirtaPtS/docker-react -f Dockerfile .dev .

script:
    - docker run -e CI=true kcirtaPtS/docker-react npm run test